name: Welcome
on:
  issues:
    types: [opened]
  pull_request_target:
    types: [opened]

permissions:
  issues: write
  pull-requests: write

jobs:
  welcome:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/first-interaction@v1
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}
          issue-message: |
            Thanks for opening your first issue! 👋
            • Read CONTRIBUTING.md for setup
            • See ROADMAP.md for priorities
            • Join Discussions to share your use case
          pr-message: |
            Thanks for your first PR! Please confirm:
            1) what/why, 2) how to test, 3) screenshots/CLI output.
